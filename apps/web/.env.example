# ---------------------------------------
# Frontend targets
# ---------------------------------------
# Game Mode: 'online' for backend-connected, 'local' for offline arcade mode
VITE_GAME_MODE=online

# Default: use Vite dev proxy for local development.
VITE_API_BASE_URL=/api
VITE_WS_GAME_URL=
VITE_WS_GAME_PATH=/api/games/ws/socket.io
# VITE_API_PROXY_TARGET=http://localhost:3000
# VITE_API_PROXY_WS_TARGET=ws://localhost:3000

# If you want to route through nginx locally, switch targets:
VITE_API_PROXY_TARGET=https://localhost
VITE_API_PROXY_WS_TARGET=wss://localhost

# OAuth redirect targets used by the gateway/service callbacks
VITE_OAUTH_SUCCESS_REDIRECT=http://localhost:5173/oauth/callback
VITE_OAUTH_FAILURE_REDIRECT=http://localhost:5173/oauth/error
