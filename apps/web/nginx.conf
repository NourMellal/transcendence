server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # SPA fallback for client-side routes (e.g., /auth/login, /oauth/callback)
        try_files $uri /index.html;
    }
}
