# Custom CRS Setup Configuration
# This file is loaded BEFORE the default crs-setup.conf to override settings

# Disable IP Reputation rules (requires GeoIP which is not compiled)
SecAction \
  "id:900130,\
   phase:1,\
   nolog,\
   pass,\
   t:none,\
   setvar:tx.do_reput_block=0"
