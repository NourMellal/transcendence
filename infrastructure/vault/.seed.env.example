# Optional Vault seed values for OAuth 42.
# Copy to .seed.env and fill in your real credentials.
OAUTH_42_CLIENT_ID=
OAUTH_42_CLIENT_SECRET=

# Optional override if you use a different redirect URI.
# OAUTH_42_REDIRECT_URI=https://localhost/api/auth/42/callback
