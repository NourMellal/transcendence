# infrastructure/vault/.seed.env.example

# Copy to .seed.env and fill with real OAuth credentials if you use 42 OAuth.
# Leaving these empty is fine when OAuth is not in use.
OAUTH_42_CLIENT_ID=
OAUTH_42_CLIENT_SECRET=
OAUTH_42_REDIRECT_URI=https://transcendence.42.demo/api/auth/42/callback
