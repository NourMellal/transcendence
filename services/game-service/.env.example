# ---------------------------------------
# Game Service runtime (non-secret, non-Vault)
# ---------------------------------------
GAME_SERVICE_PORT=3002
GAME_WS_PATH=/ws/socket.io
GATEWAY_WS_GAME_PATH=/api/games/ws/socket.io
GATEWAY_WS_CHAT_PATH=/api/chat/ws
GATEWAY_HTTP_URL=http://api-gateway:3000
# Logging: inherits LOG_DIR from repo root .env (default ./data/logs). Uncomment to override per-service.
LOG_DIR=./data/logs

# Gameplay defaults
GAME_ROOM_CAPACITY=2
GAME_TIMEOUT_MINUTES=30
SCORE_LIMIT=11
BALL_SPEED=5
PADDLE_SPEED=8

# Persistence
GAME_DB_PATH=./data/game-service.db

# External integrations
USER_SERVICE_URL=http://user-service:3001

# Redis + messaging (non-secret infrastructure)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
RABBITMQ_URL=amqp://transcendence:transcendence_dev@rabbitmq:5672
RABBITMQ_EXCHANGE=transcendence.events
RABBITMQ_QUEUE_PREFIX=game-service

# Vault access for standalone service runs
VAULT_ADDR=http://vault:8200
VAULT_TOKEN=dev-root-token
VAULT_SKIP_VERIFY=true

# Note: Internal API keys + JWT secrets are sourced from Vault only.
