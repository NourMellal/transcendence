import { describe, it } from 'vitest';

describe('HTTP game endpoints', () => {
    it.skip('creates and fetches a game via REST', async () => {
        // Placeholder for future integration test using Fastify.inject
    });
});
