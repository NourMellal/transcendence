# ---------------------------------------
# Tournament Service runtime (non-secret, non-Vault)
# ---------------------------------------
TOURNAMENT_SERVICE_PORT=3004
TOURNAMENT_SERVICE_HOST=0.0.0.0
TOURNAMENT_DB_PATH=./data/tournament-service.db
# Logging: inherits LOG_DIR from repo root .env (default ./data/logs). Uncomment to override per-service.
# LOG_DIR=./data/logs

# Downstream services
GAME_SERVICE_URL=http://game-service:3002

# Messaging defaults for local RabbitMQ
RABBITMQ_URL=amqp://transcendence:transcendence_dev@rabbitmq:5672
RABBITMQ_EXCHANGE=transcendence.events
RABBITMQ_QUEUE_PREFIX=tournament-service

# Vault access for standalone service runs (matches repo root)
VAULT_ADDR=http://vault:8200
VAULT_TOKEN=dev-root-token
VAULT_SKIP_VERIFY=true
NODE_ENV=development

# Optional fallback for local runs (prefer Vault)
INTERNAL_API_KEY=dev-internal-key
