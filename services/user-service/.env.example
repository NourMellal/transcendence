# ---------------------------------------
# User Service overrides (non-secret, non-Vault)
# ---------------------------------------
USER_SERVICE_PORT=3001
USER_SERVICE_HOST=0.0.0.0
LOG_LEVEL=info
DB_PATH=./data/users.db
REFRESH_TOKEN_TTL_DAYS=7

# Required for leaderboard aggregation (base URL of game-service)
GAME_SERVICE_URL=http://localhost:3002
# Logging: inherits LOG_DIR from repo root .env (default ./data/logs). Uncomment to override per-service.
LOG_DIR=./data/logs

# Messaging defaults for local RabbitMQ
RABBITMQ_URL=amqp://transcendence:transcendence_dev@localhost:5672
RABBITMQ_EXCHANGE=transcendence.events
RABBITMQ_QUEUE_PREFIX=user-service

# OAuth redirect targets for the SPA (not stored in Vault)
OAUTH_42_SUCCESS_REDIRECT=http://localhost:5173/oauth/callback
OAUTH_42_FAILURE_REDIRECT=http://localhost:5173/oauth/error

# Vault access for standalone service runs (matches repo root)
VAULT_ADDR=http://localhost:8200
VAULT_TOKEN=dev-root-token
VAULT_SKIP_VERIFY=true
NODE_ENV=development

# Note: Internal API keys, JWT secrets, and OAuth client credentials live in Vault.
